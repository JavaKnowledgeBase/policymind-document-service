FROM nginx:1.27-alpine

# Release image expects a prebuilt frontend dist/ folder from a native build.
COPY frontend/nginx.conf /etc/nginx/conf.d/default.conf
COPY frontend/dist /usr/share/nginx/html

EXPOSE 80
